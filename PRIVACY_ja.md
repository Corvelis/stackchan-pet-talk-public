# プライバシー

`StackChan Pet Talk` は、限定ベータ配布向けのローカル優先 Stack-chan 会話アプリです。

## ローカル処理

ローカルプロバイダーを選択し、同梱モデルが利用できる場合、以下の機能は端末内で実行されます。

- ReazonSpeech ASR と Silero VAD
- Piper Plus TTS
- llama.cpp ローカル LLM
- LiteRT-LM ローカル LLM
- OpenCV YuNet/SFace による顔のマスター認識
- CAM++ speaker embedding による声のマスター認識

マスター認識では、アプリ設定内に特徴量を保存します。これはキャラクター反応用の機能であり、本人認証、ログイン、アクセス制御ではありません。

## 外部 API

外部プロバイダーを設定した場合、アプリは関連するテキスト、音声認識対象の入力、プロンプト、会話履歴、必要に応じた画像入力を、そのプロバイダーへ送信する場合があります。

設定画面で選択できる外部プロバイダーには、以下が含まれます。

- OpenAI compatible LLM API
- OpenAI LLM API
- Gemini API
- OpenAI transcription API
- OpenAI TTS API
- Irodori TTS サーバー

API キーは、信頼できるプロバイダーに対してのみ入力してください。各プロバイダーの利用規約とプライバシーポリシーも確認してください。
Irodori TTS サーバーを利用する場合、読み上げ対象のテキスト、選択したボイス名、必要に応じてアップロードした参照音声が、設定したサーバーへ送信されます。

## カメラとマイク

アプリは、会話、ASR、TTS の制御、任意のマスター認識のために、Stack-chan のカメラとマイクのデータを利用します。また、ユーザーが画像添付やStopWatchからのリモート撮影を設定した場合は、スマートフォンのカメラを利用します。

- 顔登録では画像を撮影し、特徴量を抽出して保存します。撮影画像は一時的な実装データであり、永続的なユーザー記録として保存する意図はありません。
- 声登録では音声サンプルを集め、平均化した話者特徴量を抽出して保存します。生の音声データを永続的なユーザー記録として保存する意図はありません。
- VLM フォールバックを有効にし、画像が必要な質問をした場合、撮影画像が選択中の LLM プロバイダーへ渡されることがあります。
- StopWatchリモート撮影では、対応ファームウェアからの操作によりスマートフォンのカメラを開いて撮影します。撮影はアプリが前面にあり、ユーザーが事前にカメラを設定した場合のみ実行されます。
- リモート撮影した画像は写真ライブラリへ保存され、次の会話への画像添付として一時的に保持されます。画像を会話で送信した場合、選択中のLLMプロバイダーへ渡されることがあります。

## バックグラウンド会話

対応する Android ビルドでは、バックグラウンド会話にフォアグラウンドサービスを使用します。アプリが前面にない状態でも会話を継続するため、通知権限やバッテリー最適化除外を求める場合があります。

## 会話履歴と設定

アプリは会話、写真、なでなで、マスター認識、好感度に関する交流ログを、思い出と日記機能のために端末内へ保存する場合があります。保存された履歴は日付ごとの振り返り、写真表示、日記生成、日記の作り直しに使われます。

撮影画像は思い出機能のために端末内へ保存される場合があります。画像が必要な会話で撮影した画像、カメラボタンによる撮影、なでなで時や接続時のマスター確認画像が対象です。認識用画像は短時間に重複しないよう間引かれる場合があります。顔が見つからない、またはマスターではないと判定された認識用画像は、原則として保存されません。

日記生成では、会話履歴、写真に紐づく画像メモ、なでなでや認識の記録、キャラクター設定、好感度状態を使用します。初期設定では、日記生成のために画像そのものを LLM へ送信せず、画像メモや関連会話のテキストを使用します。外部 LLM プロバイダーを選択している場合、日記生成や作り直しのためにこれらのテキスト情報がそのプロバイダーへ送信されることがあります。

プロバイダー、プロンプト、認識しきい値、モデルパスなどの設定も保存します。API キーは、対応環境では secure storage に保存されます。

## 同梱モデル

限定ベータビルドには、モデル、辞書、ランタイムファイルが同梱される場合があります。同梱モデルとランタイムのライセンスは、アプリ内のライセンス画面から確認できます。
